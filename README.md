# commons

## Introduce

It's a simple、useful tools set for most Javaer.
- simplest API to call. such as  ``Obj.nonOr()``.
- compatible for JDK version 8.x and higher. (If you JDK under 8.x, I highly recommend you to upgrade it now. 
Because Java have a great ability to forward-compatible).
- using TDD to develop, which means the function of this tools set will under test. rare to have a bug.